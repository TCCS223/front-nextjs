import React from 'react';
import styles from './index.module.css';
import { format } from 'date-fns';
import { useRef } from 'react';

export default function FormVeiculo ({ selectedUser, setSelectedUser, isViewing, handleSubmit, Cancelar }) {return (
    <div>teste</div>
)}